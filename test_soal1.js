// Array data
const arrayToCount = [1, 2, 3, 5, 2, 8, 9, 2];
const a = 2;
const total = arrayToCount.filter(a => a === 2).length;
console.log('total data paling banyak keluar adalah ' + a + ' dengan jumlah ' + total);
